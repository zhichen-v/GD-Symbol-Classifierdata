# Copy this file to .env and set a private password before sharing the app on a network.
FRONTEND_ACCESS_PASSWORD=change-this-password

# Optional. If left empty, the access password is also used to sign browser sessions.
FRONTEND_SESSION_SECRET=
